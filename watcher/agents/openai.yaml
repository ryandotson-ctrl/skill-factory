interface:
  display_name: "watcher"
  short_description: "Inventory skills, drift, wisdom, Pulse Bus health, and ecosystem posture"
  default_prompt: "Use $watcher to produce concise ecosystem intelligence while preserving per-skill memory and Pulse Bus awareness."

policy:
  allow_implicit_invocation: true
