interface:
  display_name: "watcher"
  short_description: "Maintains the skill ecosystem, Pulse Bus, self-improvement loops, migration safety, and portable skill governance."
  default_prompt: "Use $watcher for Keep the entire skill ecosystem coherent, lean, safe, discoverable, and continuously improving."

policy:
  allow_implicit_invocation: true
