interface:
  display_name: "reliability"
  short_description: "Stabilizes incidents, runtime failures, performance regressions, async hazards, logs, lifecycle issues, and resource budget failures."
  default_prompt: "Use $reliability for Restore service first, identify the real failure mode, then harden the path against recurrence."

policy:
  allow_implicit_invocation: true
