interface:
  display_name: "automation"
  short_description: "Drives browser, desktop, screenshot, Atlas, Playwright, and visible verification workflows when direct observation matters."
  default_prompt: "Use $automation for Use controlled interaction and visual evidence to verify real behavior instead of guessing."

policy:
  allow_implicit_invocation: true
