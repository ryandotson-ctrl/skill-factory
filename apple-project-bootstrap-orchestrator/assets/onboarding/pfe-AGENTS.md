# Project Free Energy Apple Companion Guidance

- Use `$skill_director` as the ecosystem intelligence lead before major project-structure changes.
- Use `$runtime-context-launcher` for PFE runtime bring-up and launch-truth validation.
- Use `$model-ux-orchestrator` for app-facing truth, status, and response-lane UX decisions.
- Use `$qa-automation-engineer` for regression gates, including real app-target build proof.
- Use `$apple-ecosystem-release-operator` for release-profile, archive, and TestFlight work.
- Keep companion Apple apps runtime-isolated from sibling Apple bundle identities unless that sharing is explicitly audited.
