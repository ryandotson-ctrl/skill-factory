{
  "skill_id": "runtime-context-launcher",
  "version": "2.5.0",
  "description": "Detects PFEMacOSApp runtime context, launches the backend, and verifies chat-ready bundle-scoped ownership before trusting startup",
  "inputs": [
    {
      "event_type": "skill:runtime-context-launcher:requested",
      "schema_ref": "default_event_schema"
    },
    {
      "event_type": "runtime:context_launch_requested",
      "schema_ref": "default_event_schema"
    }
  ],
  "outputs": [
    {
      "event_type": "skill_activity:runtime-context-launcher",
      "schema_ref": "default_finding_schema"
    }
  ]
}
