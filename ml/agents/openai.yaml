interface:
  display_name: "ml"
  short_description: "Builds, tunes, evaluates, optimizes, quantizes, and stabilizes local and cloud model systems."
  default_prompt: "Use $ml for Make model systems measurable, stable, efficient, and honest about hardware and runtime constraints."

policy:
  allow_implicit_invocation: true
