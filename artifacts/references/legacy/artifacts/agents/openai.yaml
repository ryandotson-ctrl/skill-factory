interface:
  display_name: "artifacts"
  short_description: "Creates, inspects, converts, validates, and packages PDFs, slides, spreadsheets, images, logos, and ChatGPT Apps."
  default_prompt: "Use $artifacts for Produce polished user-visible artifacts with validation, rendering checks, and packaging discipline."

policy:
  allow_implicit_invocation: true
