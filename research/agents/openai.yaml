interface:
  display_name: "research"
  short_description: "Produces current, source-grounded research, official-doc interpretation, technology freshness checks, SOTA scans, and decision intelligence."
  default_prompt: "Use $research for Convert uncertain or time-sensitive questions into cited, current, decision-ready intelligence."

policy:
  allow_implicit_invocation: true
