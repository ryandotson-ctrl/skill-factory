interface:
  display_name: "security"
  short_description: "Audit AppSec, privacy, threat models, ownership, macOS, and network posture"
  default_prompt: "Use $security to protect systems and data with evidence-led defensive security analysis."

policy:
  allow_implicit_invocation: true
