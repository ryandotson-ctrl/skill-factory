# Profile Boundaries

## Portable Core
- default to local-only posture
- verify listener exposure, auth boundaries, and writable runtime data paths
- report hardened configuration without host-specific assumptions

## PFEMacOS Profile
- apply PFEMacOS UDS-first transport and session-token contracts
- keep loopback as compatibility fallback, not primary architecture
