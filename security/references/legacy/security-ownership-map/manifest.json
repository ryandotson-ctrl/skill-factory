{
  "skill_id": "security-ownership-map",
  "version": "1.0.0",
  "description": "Build a security ownership topology from git history and identify bus-factor risks in sensitive code.",
  "inputs": [
    {
      "event_type": "skill:security-ownership-map:requested",
      "schema_ref": "default_event_schema"
    },
    {
      "event_type": "security:ownership_map_requested",
      "schema_ref": "default_event_schema"
    }
  ],
  "outputs": [
    {
      "event_type": "security:ownership_map_ready",
      "schema_ref": "default_event_schema"
    },
    {
      "event_type": "skill_activity:security-ownership-map",
      "schema_ref": "default_finding_schema"
    }
  ]
}
